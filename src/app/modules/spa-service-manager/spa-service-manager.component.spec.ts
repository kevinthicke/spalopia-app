import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SpaServiceManagerComponent } from './spa-service-manager.component';
/* 
describe('SpaServiceManagerComponent', () => {
  let component: SpaServiceManagerComponent;
  let fixture: ComponentFixture<SpaServiceManagerComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SpaServiceManagerComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SpaServiceManagerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */